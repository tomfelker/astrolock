astropy >= 5.2
numpy
# tkinter ships with Python as stdlib; it is NOT installable via pip (used by the legacy GUI)
pygame-ce
requests
pyserial
skyfield
torch
pandas
imgui-bundle    # seeker GUI (astrolock.seeker.gui_imgui): Dear ImGui + ImPlot python bindings
moderngl        # seeker GUI: GPU debayer/WB/gamma of raw camera frames
glfw            # seeker GUI: window + event loop (post_empty_event wake on new frames)
zwoasi
zwoeafpy        # ZWO EAF focuser SDK binding (astrolock.seeker.eaf; ctypes over EAF_focuser.dll)
psutil          # seeker scheduling: cross-platform process priority + CPU affinity (backend --sched)

